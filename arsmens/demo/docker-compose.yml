version: '3.8'

services:
  streamlit-demo:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8501:8501"
    container_name: asturias-demo-app
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
